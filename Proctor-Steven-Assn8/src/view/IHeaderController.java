package view;

/**
 * IHeaderController defines the interface required for a controller, in order for the Header to update the controller of user input
 *
 * @author Steven Proctor
 * @version 1.0
 */
public interface IHeaderController {

    /**
     * Resets the game
     */
    void reset();
}
